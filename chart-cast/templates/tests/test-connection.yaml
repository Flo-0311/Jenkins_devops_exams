apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-cast.fullname" . }}-test-connection"
  labels:
    {{- include "chart-cast.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-cast.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
