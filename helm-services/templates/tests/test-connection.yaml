apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-services.fullname" . }}-test-connection"
  labels:
    {{- include "helm-services.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-services.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
