apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-services.fullname" . }}
  labels:
    {{- include "helm-services.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-services.selectorLabels" . | nindent 4 }}
